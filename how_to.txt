git init

git branch	# checks branch

git add .

git commit -m "message"

git remote add origin https://github.com/P48L0-P1C4550/the_socket.git

git push origin master

git clone https://github.com/P48L0-P1C4550/the_socket.git

####

git status

git log

git branch new-branch-name

git checkout new-branch-name	#switch

git checkout master		#change before merging to master
git merge new-branch-name	#merge to the one ur in

git log --oneline
